# TaskLog 2026-07-06｜個人/行政拆分與請假整合 [Claude@Win]

> todo 真相源。當日主題：ScrToday/ScrAdmin 改版、個人與行政查核拆分、回覆 5 選項、請假整合流程指南。

## 今日完成
- **拆分決定（B 案）**：行政查核獨立成**新 Power App**（不與個人時程同 app，解掉「橫向鎖定 vs 手機」衝突）。ScrToday 移除「行政查核」鈕（`btnAdmin`/`Navigate(ScrAdmin)`）。
- **ScrToday**（`workingfiles/canvas/ScrToday_paste.pa.yaml`）：頭部改全寬修標題/姓名重疊；回覆選項 4→**5**（新增 `4. 延遲（09:30沒到公司，當天已請假）`，原「其他」順移為 `5.`）。
- **ScrAdmin**（`workingfiles/canvas/ScrAdmin_table.pa.yaml`）：獨立 app 版——移除返回鈕、頂部單「↻ 重新整理」；4 欄（編號6pt｜姓名｜**出勤狀態粗體藍=已確認存檔**｜行政輸入下拉+自訂｜回覆）；響應式列高 `TemplateSize=(Parent.Height-116)/25`；列交替底色+分隔線；藍字**只在 SharePoint 寫入成功後**才顯示（非樂觀，避免誤判已存）。
- **請假整合建置指南**：`workingfiles/flows/Flow_請假整合_建置指南.html`。涵蓋 5 處：App(5選項)／0945(行事曆判請假→寫`field_7=4`→排除寄信)／0955(收斂 1或4→**1或5**、排除請假發卡)／1005-1030(排除請假)／1000彙整通知行政(排除請假)。每處標明基本/進階模式、右值填法、動作位置。**§3-1 已照 0630 實際匯出改精確 before→after**：`本列原勾選1或4` Compose 把 `'4.'→'5.'`；`本列未回覆` 加 `not(startsWith(…,'5.'))`。

## 待辦（真相源）
- [x] **1000 有誤，已定位並寫入修法**（2026-07-07）：確認為 `1000_米米信主旨寫入清單` 流程（非彙整通知行政那支），`套用至每一個 → FilterEvents` 判斷「請假涵蓋10:00」只認主旨含「10:00」或「全天」字樣，跨日年假格式（`X請年假，date至date，共N時，代理人:Y`）不含此二字樣，永遠不命中，導致該人被誤判無請假（`workingfiles/automate_export/1000_清單__米米信行程通知寫入清單_20260707023203.zip` 對照 `workingfiles/0706_today` 擷取證實）。修法＝加一個 or 分支判事件 start/end 相差 >1 天（跨日整天事件），已寫入 `workingfiles/flows/Flow_1000_米米信主旨寫入清單_建置指南.html`「2026-07-07 修正」節，**待使用者在 Portal 套用**。
- [x] **0945 同款跨日年假漏判，已定位並寫入修法**（2026-07-07）：以新匯出 `0945_清單_每日專案延誤通知EMAIL_v1_0707_20260707031727.zip` 核對，`FilterEvents_本人請假` where 條件與 `Flow_請假整合_建置指南.html` §2-2 原文一致（已照指南套用），但與 1000 米米信同一根因——跨日年假格式不含「10:00/全天」字樣，**且**此處多一層「主旨含今天日期」AND 守衛，對跨日事件只在請假起始日當天成立，兩條件同時擋下，導致跨日請假者**每天**被判無請假、繼續收到延遲信、field_7 不會標 `4.`。修法同款（加 or 分支：事件 start/end 相差 >1 天），已寫入 `Flow_請假整合_建置指南.html` §2-2（含浮動修改待辦清單），§5-2「1000工作延遲彙整→行政 email」有同一風險但該流程現況未確認是否仍在用，已加警告待你確認。**待 Portal 套用**。
- [x] **另一根因：取得行事曆檢視 時間參數尾碼 `+08:00` 撈到昨天的單日全天假**（2026-07-07，使用者實測指出「這筆不該有」發現）：與 §2-2 無關的獨立問題——此行事曆全天事件一律用 **UTC 午夜**當邊界，時間參數尾碼用 `+08:00`（台北當日換算 UTC）會產生 8 小時重疊帶，導致查「今天」卻連「昨天」的單日全天假（如 `張耀中請年假，2026-07-06(全天)`）都撈進來（雖然下游 FilterEvents 的日期字面比對仍會正確排除，不影響最終結果，但撈到的資料本身不乾淨）。修法＝尾碼 `+08:00`→`Z`（台北日期字面數字直接當 UTC 邊界），已驗算 4 筆真實資料全部正確（單日事件不再撈到、跨日事件仍正常撈到）。已寫入 `Flow_1000_米米信主旨寫入清單_建置指南.html` §2b、`Flow_請假整合_建置指南.html` §2-1，**待 Portal 套用**。
- [x] **§5「1000工作延遲彙整→行政 email」修法已移除**（2026-07-07，使用者指示）：此流程排定大修，不再疊補丁，`Flow_請假整合_建置指南.html` §5 內容已清空待重建規格。
- [x] **1000 米米信已在 Portal 套用兩項修法（Z 尾碼＋FilterEvents 跨日分支），流程正常執行**（2026-07-07 使用者確認）。
- [ ] 各流程依指南實際在 Power Automate Portal 套用：0945（§2-1 時間尾碼＋§2-2 跨日年假修正、§2-4 巢狀迴圈寫 field_7=4 的位置＝`要寄嗎` 條件同層之後）、0955（§3-1 改號）、1005/1030、1000彙整（排定大修，暫不套本指南舊版）。1010（同款米米信邏輯，是否需要同兩項修法待使用者提供匯出檔核對）。
- [x] **已定案（2026-07-07）：短時段請假（如 09:30-10:00）涵蓋 10:00 → 整天抑制延遲信＋field_7 標「當天已請假」，維持現狀不改**。使用者確認接受此粗顆粒度（不分長短假），非 bug，不必加時長門檻。範例：`蔡宇翔請年假，2026-07-06(09:30-10:00)`。
- [ ] ScrToday／ScrAdmin 兩份 yaml 貼上實測（ScrAdmin 另建新 app：連 AttendanceHistory＋team_member、方向橫向、關 Scale-to-fit）。
- [ ] A 案（另建 DailyStatus 每人一列清單，填一次+即時）暫緩——已選 B（app 填一次但寫入 ~1–2s）。

## 未提交
- 今日 `ScrToday_paste.pa.yaml`、`ScrAdmin_table.pa.yaml`、`Flow_請假整合_建置指南.html` 變更**尚未 commit**（待使用者確認後 commit＋merge main）。
