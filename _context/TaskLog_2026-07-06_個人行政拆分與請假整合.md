# TaskLog 2026-07-06｜個人/行政拆分與請假整合 [Claude@Win]

> todo 真相源。當日主題：ScrToday/ScrAdmin 改版、個人與行政查核拆分、回覆 5 選項、請假整合流程指南。

## 今日完成
- **拆分決定（B 案）**：行政查核獨立成**新 Power App**（不與個人時程同 app，解掉「橫向鎖定 vs 手機」衝突）。ScrToday 移除「行政查核」鈕（`btnAdmin`/`Navigate(ScrAdmin)`）。
- **ScrToday**（`workingfiles/canvas/ScrToday_paste.pa.yaml`）：頭部改全寬修標題/姓名重疊；回覆選項 4→**5**（新增 `4. 延遲（09:30沒到公司，當天已請假）`，原「其他」順移為 `5.`）。
- **ScrAdmin**（`workingfiles/canvas/ScrAdmin_table.pa.yaml`）：獨立 app 版——移除返回鈕、頂部單「↻ 重新整理」；4 欄（編號6pt｜姓名｜**出勤狀態粗體藍=已確認存檔**｜行政輸入下拉+自訂｜回覆）；響應式列高 `TemplateSize=(Parent.Height-116)/25`；列交替底色+分隔線；藍字**只在 SharePoint 寫入成功後**才顯示（非樂觀，避免誤判已存）。
- **請假整合建置指南**：`workingfiles/flows/Flow_請假整合_建置指南.html`。涵蓋 5 處：App(5選項)／0945(行事曆判請假→寫`field_7=4`→排除寄信)／0955(收斂 1或4→**1或5**、排除請假發卡)／1005-1030(排除請假)／1000彙整通知行政(排除請假)。每處標明基本/進階模式、右值填法、動作位置。**§3-1 已照 0630 實際匯出改精確 before→after**：`本列原勾選1或4` Compose 把 `'4.'→'5.'`；`本列未回覆` 加 `not(startsWith(…,'5.'))`。

## 待辦（真相源）
- [ ] **⚠ 1000 有誤，尚未修改**——使用者回報，晚點自行處理（推測指指南 §5「1000工作延遲彙整_通知行政email」排除請假那段，或 1000 米米信；待使用者釐清並修正）。
- [ ] 各流程依指南實際在 Power Automate Portal 套用：0945（含 §2-4 巢狀迴圈寫 field_7=4 的位置＝`要寄嗎` 條件同層之後）、0955（§3-1 改號）、1005/1030、1000彙整。
- [ ] ScrToday／ScrAdmin 兩份 yaml 貼上實測（ScrAdmin 另建新 app：連 AttendanceHistory＋team_member、方向橫向、關 Scale-to-fit）。
- [ ] A 案（另建 DailyStatus 每人一列清單，填一次+即時）暫緩——已選 B（app 填一次但寫入 ~1–2s）。

## 未提交
- 今日 `ScrToday_paste.pa.yaml`、`ScrAdmin_table.pa.yaml`、`Flow_請假整合_建置指南.html` 變更**尚未 commit**（待使用者確認後 commit＋merge main）。
