# TaskLog 2026-08-03｜全案未結待辦盤點 [Claude@Mac]

> **待辦真相源**。本檔由 2026-08-03 `_context` 整理產生：把 06-08～07-28 共 10 份 TaskLog 的未結項目全部收攏到這一份，原檔已改前綴 `ClosedTaskLog_` 移入 `_context/archive/`。
> 每項標明「來源檔」，需要細節就開 archive 裡那份。
> 全案需求與架構全貌見 `_context/PRD_2026-08-03_出勤查核系統.html`。

---

## PO 裁示（2026-08-03，第一輪）
| 項目 | 裁示 |
|---|---|
| A2 清單層硬權限 | **下一階段處理** |
| A3 清單保留與搬移 | ✅ **定案**：用 Power Automate **排程**（非手動按鈕）每週搬移**兩週前**的紀錄到 Excel，**清單最多保留兩週紀錄** |
| A4 Planner 卡片就地改狀態 | **下一階段處理** |
| A5 模組二／三 | **下一階段處理** |
| A7 發信報告行程 | 改為**米米信代發解析**：已派子代理整理「同事姓名 ↔ 米米信暱稱對照表」，對照表完成後再寫流程 |
| A8 1005／1030 前提 | ✅ **已解**：準時者由行政在 09:30、10:00 兩次於出勤查核 App 輸入（見 `informaiton/TaskDB_0803.xlsx`「流程」工作表） |
| A10 週報加請假 | 稍後處理 |
| B1 ScrAdmin yaml | ✅ **已貼上、運作正常**（使用者 2026-08-03 回報） |
| A1 field_8 粒度 | ✅ **定案：解法 A ── 同一人同一天多列，出勤狀態全部填一樣的值（fan-out）** |
| A6 名冊缺漏告警 | ✅ **要做**（使用者：可新增）；作法接在 0945 尾端，不動既有動作 |
| A9 0930 群組範圍 | ✅ **定案：維持掃全部群組**（重點是掃公司所有人的工作狀況），改用容錯＋失敗告警處理 429 |
| A11 卡片留白 | 使用者認為已改；repo 版 `ScrToday_paste.pa.yaml` 仍是 `TemplateSize: =420`，需匯出 Studio 現況比對 |

## PO 裁示（2026-08-03，第二輪：巡檢機制改造）
| 項目 | 裁示 |
|---|---|
| 巡檢不得改寫同事回覆 | ✅ **定案**：Automate 只更新 Planner 卡片即時狀態，不得覆蓋 `field_7`（表單回覆） |
| 新增欄位 | ✅ **已建**：清單加 `PlannerStatus`（內部名確認自 FldEdit 網址），值只有「已更新」／「延遲」 |
| 0945 請假預填 | ✅ **關閉**：不再自動寫 `field_7='4.'`／`field_8`；連帶移除寄信條件中的 `not(本人請假)` |
| 請假者義務 | ✅ **裁示**：請假人員仍須填寫儀表板（本應休假前就安排工作、調整卡片截止日）→ 通知對象**不排除**請假者 |
| 通知管道統一 | ✅ **定案**：0955／1015 都改發 LINE（寄信到 `domain.e` 轉發），1015 不再發 Planner 卡片 |
| 通知條件 | ✅ **定案**：`PlannerStatus='延遲'` 且回覆為 1./5./未回覆；勾 2、3、4 不發 |
| 1030 週報 | ✅ **定案**：改讀清單（不再讀 Excel）＋上週區間篩選；統計基準統一為 `field_7` |
| 選項文字 | ✅ **定案**：「3. 延遲（09:30沒到公司，**待**填寫假卡）」（原「請填寫假卡」） |
| 行政頁顯示 | ✅ **定案**：加「Planner 狀態」欄；符合通知條件者整列紅字 |

---

## A. 重大待決（需 PO 拍板，沒拍板下游做不下去）

### A1. `實際出勤狀態`（field_8）儲存粒度 ✅ 定案：解法 A（fan-out，全部列填一樣的值）
- **裁示（2026-08-03）**：同一人同一天有多列時，出勤狀態**每列都填相同的值**。
- **現況查核（2026-08-03 讀碼確認，非推測）**：
  - 行政 App `ScrAdmin_權限閘_paste.pa.yaml:232／265`：`UpdateIf(AttendanceHistory, Email = ThisItem.Email && 日期 = gToday, {實際出勤狀態: …})` ── **已經是 fan-out**，寫該人今日全部列。
  - 1000 米米信流程：`Flow_1000_米米信主旨寫入清單_建置指南.html` 三決策①已明訂「今日該人**所有列**都填」，實作是 Apply to each **逐清單列**跑、每列各自比對寄件者 Email 後各自寫入 ── **設計上也是 fan-out**。
  - 結論：兩條主要寫入路徑設計上都已符合裁示。先前觀察到的「只填一列」若仍存在，是**實作或資料層問題，不是設計缺陷**。
- **待驗（不做完不能宣稱這條已結）**：
  - [ ] 找一個今日有 2 個以上延遲任務的同事，實測 1000 流程跑完後是否每列都有 field_8。
  - [ ] `1010` 那支流程**未取得匯出檔**，是否同樣逐列寫入未確認 ── 若它只寫一列，會把 1000 已 fan-out 的結果覆蓋成不一致。
  - [ ] A7 米米信代發解析上線後，寫入同樣要對「被代報者今日所有列」fan-out。
- 未採用的三案（留紀錄，避免日後重議）：B 拆獨立「每日出勤狀態」清單／C 只在備份時補／D 清單只填一列、畫面表頭顯示一次。
- 來源：`ClosedTaskLog_2026-06-16`（2026-07-01 待辦段）＋2026-08-03 PO 裁示與讀碼查核

### A2. 資料層硬權限：清單層鎖 — 已知安全缺口
- 現況只有 **App 內 UI 權限閘**（`locIsAdmin` 讀 team_member），懂技術者可繞畫面直接讀寫清單。
- 待決：是否做 SharePoint 項目層權限＋寫入導向 server-time flow，白名單 1000／1010 兩支 flow 帳號，且不擋員工寫自己的 field_7。使用者 07-23 說「稍晚提醒」，至今未動。
- 併同一件事：**App Owner `真核域(開會用)` 疑為多人共用帳號＝權限漏洞**，建議換個人帳號。
- 來源：`ClosedTaskLog_2026-07-23`、`ClosedTaskLog_2026-06-16`

### A3. 清單資料保留與每週搬移機制 ✅ 已定案（2026-08-03）
- **裁示**：Power Automate **排程**（非手動按鈕）每週執行，把**兩週前**的紀錄搬到 SharePoint 上 `TaskDB.xlsx` 的「歷史紀錄_表單回覆＋實際出勤」工作表，**清單最多保留兩週紀錄**。
- 待建置細節（可自行決定、不必再拍板者標「自訂」）：
  - 排程星期／時段：自訂，避開 0930~1030 尖峰（建議週六或週日）。
  - 搬移範圍判定：用 `Title` 前綴 `yyyyMMdd_` 比對（不對 `日期` 欄下 `$filter`，既定原則）。
  - **順序鐵律**：先 append 成功、驗證列數相符，才刪清單列；刪不可與寫同一迴圈。
  - append 目標是 9 欄的「歷史紀錄_表單回覆＋實際出勤」表，不是舊的 11 欄 `TaskDB` 表。
- **手動清理進度（2026-08-04 使用者回報）**：Excel `TaskDB.xlsx` 的「歷史紀錄_表單回覆＋實際出勤」工作表**已清理資料到 2026-08-03**。
  - [ ] **2026-08-07（本週五）再清理一次** ← 排程機制上線前的人工暫代
- 副作用：清單只留兩週後，模組二／歷史查詢必須改讀 Excel（不可委派＋2000 列上限）——這條要在 A5 開工時一併設計。
- 取代：原手動觸發的 `Flow_清單轉TaskDB備份_建置指南.html`、`Flow_清單轉WeekDB複製_建置指南.html`。
- 來源：`ClosedTaskLog_2026-06-16`（07-06 段）＋2026-08-03 PO 裁示

### A4. 下一階段功能：儀表板帶入 Planner 卡片、就地改狀態
- 使用者 2026-07-31 裁定為下一階段主題。
- **已確認事實**：清單 `Title` 第三段就是 Planner task id（28 字元 base64url ＝ Graph `plannerTask`）。**結尾底線是 id 本身的字元**，用 `Split` 取最後一段會拿到空字串；要取用 `Mid(Title, Len(Email) + 11)`，更建議讓 09:30 flow 直接多寫一欄 `PlannerTaskId`。
- **待決做法二選一**：
  - App 直接接 Planner 連接器 → 修改者顯示為本人（稽核正確），但連接器不可委派、每人首次要授權。
  - 呼叫 flow 代改 → 免授權，但異動全掛同一帳號、稽核失真。
- **待確認**：同事是否都是該 Planner 計畫的成員（非成員會拿到權限錯誤）。
- **建議先做最小驗證**：加連接器＋一顆測試按鈕寫死 task id 設完成度 100，確認能改、修改者是本人、非 owner 的同事不被擋。
- 來源：`ClosedTaskLog_2026-07-28`

### A5. 模組二（請假紀錄查詢）／模組三（假期餘額）是否啟動
- ScrLeave 畫面待建，卡在使用者未提供 6 項資料：請假 Excel 站台／檔名／Table 名、欄位清單、對應鍵（Email 或員工編號）、餘額是否已算好（否則配額−已用）、外部請假表單 URL。
- 已定案設計：Excel 直連唯讀不建清單（唯讀＋<2000 列＋低頻）；請假申請走外部既有表單，ScrLeave 只放 `Launch(URL)` 按鈕。
- 來源：`ClosedTaskLog_2026-06-16`

### A6. 0945 名冊缺漏告警 ✅ 要做（2026-08-03 使用者裁示）
- 風險：team_member 名冊漏人 → 該延遲者漏寄通知且**沒有任何人會發現**（流程照樣回報成功）。
- **複雜度評估（回答使用者「流程會不會變複雜」）**：接在 0945**尾端的獨立分支**，既有動作一個都不動——
  1. 篩選陣列：從今日延遲列取出 Email，篩出「不在名冊 Email 集合裡」的；
  2. 條件：結果非空才往下；
  3. 寄一封告警信到 `domain.e`。
  共 3 個動作，與主線無耦合，壞了也只是不寄告警、不影響正常通知。
- **必守**：告警主旨用 `0945排程告警_名冊缺漏`，**不可用 `工作延遲_` 前綴**——Planner2Line 靠主旨前綴路由，用錯會被當成某個人的個人通知發到 LINE。
- **查證結果（2026-08-04）**：掃過全部 12 支線上版與 5 支改造版流程，**沒有任何一支含「告警」字串——這封信完全還沒實作**。A6、A9 兩條都停在規劃階段，`0945排程告警_` 只是先定下的命名。
- **與 0955 主旨事故的關聯**（見 B0-fix）：用錯前綴的後果跟 0955 是同一機制的兩個方向——0955 是主旨多了文字導致**該發沒發**，告警信若誤用 `工作延遲_` 前綴則是**不該發卻發給某個同事**。動工前先讀 B0-fix 的主旨契約表。
- **建議施作時機**：A6 這支邏輯單純（3 個動作、與主線無耦合），可接在 0945 改造版驗證通過後一併處理。
- 設計已寫在 `Flow_0945_延遲通知信_改讀清單_建置指南.html` 附錄。
- 與 A9 的失敗告警可共用同一封告警信機制。
- 來源：`ClosedTaskLog_2026-06-22_0945名冊版`＋2026-08-03 PO 裁示

### A7. 米米信代發解析（原「發信報告行程」需求，2026-08-03 具體化）
- **需求**：一封米米信可能同時幫多位同事報備，現況只會寫進寄件者本人的列，被代報的同事 field_8 仍是空的 → 會被 1005／1030 誤判成未回報。
- **裁示**：分兩階段。**階段一（進行中）** 就現有清單語料整理「同事姓名 ↔ 米米信暱稱」對照表；**階段二** 對照表確認後才設計解析流程（多人命中 → fan-out 寫入多位同事的 field_8）。
- **階段一已完成（2026-08-03 子代理產出）**：`workingfiles/整理資料/米米信暱稱對照表_2026-08-03.md`／`.csv`（BOM UTF-8）。
  - 語料：1940 列中 597 筆 `**` 主旨、去重 355 種；前綴變體只有 `**` 與 `** `，無全形星號。
  - 覆蓋：19 人全有語料並偵測到暱稱，信心高 18 人、中 1 人（黃文郁）。
  - **待人工確認 3 件**：
    1. **`聯絡人` 名冊只有 18 人，盛浩偉（haoweisheng@techartgroup.com，54 筆語料）不在名冊** ── Email 由歷史紀錄反推。這正是 A6 名冊缺漏告警要抓的情況，需確認是否補登。
    2. 衝突暱稱 2 個（慧修、儀蘋）：成因不是暱稱歧義，而是**行政黃文郁代發、主旨只寫被代報者名字，系統依寄件者寫進她自己的列**——即代發問題的實例。
    3. 4 個推測暱稱無單獨自報樣本：`Ivy/ivy`→塗文鳳、`小六`→黃文郁、`宏旗`→陳宏錡（錯字）、`Edward`→方旭。
  - 疑似代發 19 種主旨／43 列，其中 **6 種有缺漏**（主旨提到但那人沒拿到狀態），例：`**建興 宏旗 Ray Ed去新竹拍攝` 缺楊睿謙、`**文鳳 宇翔 林軒去佛光山場勘+投影定位` 缺林軒、`**昱丞、怡惠、儀蘋上午在存保` 缺林昱丞。
  - 未能對應 4 筆，全是林昱丞省略自稱直接寫行程（`**去陽明山定檢`）→ **解析規則必須保留「退回寄件者 Email」的 fallback**。
  - 切分陷阱（寫流程時必看）：吳怡萱暱稱是數字 `51`（前後不可接數字）；林軒是唯一用全名且格式為 `**HHMM林軒…`（時間在暱稱前）；Ed／Sunny／Ivy 大小寫混用；陳怡惠、蔡宇翔常把數字或動作黏寫無空格；林寬祐愛用全形冒號。
- **PO 定案規則（2026-08-03）**：① 主旨中出現的人名 → 更新那些人的 field_8（值＝完整主旨），當天多列一併更新；② **寄件者若沒出現在主旨裡就不填他自己**；③ 主旨完全沒人名 → 退回填寄件者本人；④ 常見錯字（如「宏旗」）也要進對照表。
- **盛浩偉 2026-07-03 已離職**（最後工作日同日）→ 不補登名冊，對照表不列他；歷史語料 54 筆不受影響。A6 的「名冊缺漏」疑慮就此解除。
- **階段二已產出（2026-08-03）**：
  - `workingfiles/flows/Flow_1000_米米信代發解析_建置指南.html`（就地改造 `1000_清單__米米信行程通知寫入清單`，基準＝2026-08-03 匯出檔的 definition.json，含 6 項浮動修改待辦）。
  - `workingfiles/整理資料/team_member_米米信暱稱欄_2026-08-03.csv`（18 列，BOM UTF-8）。**暱稱直接加在既有 `team_member` 表格上**（PO 2026-08-03 指示：不另建表），新增三欄 `米米信暱稱1/2/3`；一個人最多 3 個別名，用單層 `or()` 比對，效能與現行一致。
  - **名冊位置已查證（2026-08-03，掃 12 支流程匯出檔）**：`聯絡人`＝**Excel 不是清單**——`TaskDB.xlsx` 的「聯絡人」工作表內有表格 `team_member`（範圍 A1:E19、18 人、GUID `{67870985-434E-4538-AC8D-3C6A5B409136}`）。只有 **0920**（補員工編號）與 **0945**（逐人寄信）讀它，都走 Excel Online (商務) 的 GetItems；沒有任何流程用 SharePoint 清單當名冊。Power Apps 兩個畫面也是讀這張 Excel 表判權限。
  - **`出勤存取權限`＝「行政管理」現為 3 人**（黃文郁、塗文鳳、陳怡惠），比 07-23 記錄的 2 人多了塗文鳳。
  - **可直接匯入的流程套件** `workingfiles/automate_import/1000_米米信代發解析_v2_匯入用_20260803.zip`：由 2026-08-03 匯出的 1000 套件改造，換流程 GUID 與顯示名（`1000_清單__米米信代發解析_v2`）、manifest 補 Excel Online (商務) 連線相依（照 0945 套件寫法）、`suggestedCreationType` 設 `New`。Excel 的檔案與表格識別碼直接寫死 `team_member` 的實際值（取自 0920／0945 匯出檔），匯入後不必手動重選。已驗：引用的動作名稱全部存在、`FilterEmails` 無殘留、runAfter 串接無斷點；**未驗**：實際匯入與實跑。
  - **匯入前必做**：`team_member` 先加好 `米米信暱稱1/2/3` 三欄並填值。欄位沒加就匯入**不會報錯**，但每封信都走 fallback、代發完全失效。
  - 新舊兩支都 10:00 觸發、都寫 field_8，**不可同時啟用**；測試期間關新流程排程只手動執行，測通後關舊流程。
  - 架構：先逐信解析出「這封信要寫給誰」建成陣列變數 `varStatusMap`，再逐清單列查表寫入 → 天然 fan-out，同時滿足 A1 的「多列填一樣的值」。
  - **待驗證兩點**：`setProperty` 函式在此租戶可用性（附備援寫法）；`first(篩選_本列狀態)` 是否取到最新一封信。
- **1010 必須跟著改**：架構相同，若仍是舊的「只認寄件者」邏輯，會把 1000 剛 fan-out 的結果覆蓋掉一部分。匯出檔已在 `informaiton/1010_清單__第二次米米信行程通知寫入清單_20260803084034.zip`。
- 既有原則不變：**沒解析出人名時歸屬靠寄件者 Email**——代發是這條原則的例外，不是全面改成靠主旨解析。
- 來源：`ClosedTaskLog_2026-06-16`（06-26 待辦）＋2026-08-03 具體化

### A8. 1005／1030 提醒的前提條件 ✅ 已解（2026-08-03）
- 疑慮是：對象若是「今日全體」，會不會誤提醒一票正常上班、只是沒發米米信的人。
- **答案在 `informaiton/TaskDB_0803.xlsx`「流程」工作表**：行政在 **09:30**（誰已到公司）與 **10:00**（到公司的人）各輸入一次，準時者當下就會有 field_8 值，所以 10:05 仍空白＝真的沒回報也沒被行政記到。前提成立，不必另建標記機制。
- 「流程」表同時確認 10:05 目前是**人工**處理（行政用 LINE 請對方補發 `**`），10:30 未回應才發提醒卡——1005／1030 兩支流程就是把這段人工自動化。
- ~~仍待確認：1005 收件者 domain.e 與 Planner2Line 加 `米米提醒_` 前綴路由。~~ ✅ **已解（2026-08-04）**：`米米提醒_{人名}`（日期選填）解析端 2026-07-02 就實作好了，`type: "mimi"`＋`mimi_message_template`，與工作延遲共用轉發流程。詳見 B0-fix 段的「米米提醒」小節。**1005／1030 可直接建，前提條件全部滿足。**
- 來源：`ClosedTaskLog_2026-06-16`（07-01 待辦）＋`informaiton/TaskDB_0803.xlsx`「流程」表

### A9. 0930 Planner 節流 429 ✅ 定案：維持全部群組，改走容錯＋告警
- **裁示（2026-08-03）**：**不縮群組範圍**——重點就是要掃到公司所有人的工作狀況，只鎖 PLANNER_CK 會漏抓其他群組的延遲工作。
- 因此 429 只能靠容錯處理：對 `列出群組的方案 (ListGroupPlans)` 設「設定執行後」允許失敗繼續，並加重試／延遲。
- **必須配套失敗告警，不可只做容錯**：單純容錯＝某個群組整組靜默漏抓，比現在「整支流程失敗」更危險（現在至少看得到失敗）。做法＝把失敗的 groupId 收集成陣列，跑完若非空就寄告警信（與 A6 共用同一封告警信機制）。
- **告警信現況（2026-08-04 查證）：尚未實作**，全案沒有任何流程含告警動作。主旨命名與前綴禁忌見 A6 與 B0-fix 的主旨契約表。
- 來源：`ClosedTaskLog_2026-06-16`（06-26 段）＋2026-08-03 PO 裁示

### A10. 週報加請假
- 走既有群組彙整模式（方案 1，已定），把「請假紀錄」加進彙整內容；逐人版（方案 2）不做。
- **卡點**：週報的發送端流程不在 Planner2Line repo，需使用者匯出該流程 JSON 才能延伸。
- 來源：`ClosedTaskLog_2026-06-16`

### A11. ScrToday 卡片留白 — repo 與 Studio 可能已不同步
- 使用者 2026-08-03 表示「這應該已經改了」。**查核結果**：repo 的 `workingfiles/canvas/ScrToday_paste.pa.yaml:90` 仍是 `TemplateSize: =420`，六個 Label 的 `AutoHeight: =true` 都在。
- 我看不到 Studio 現況。兩種可能：① 只是覺得目前留白可接受、沒真的改數值 → 這條可直接結案；② 在 Studio 手動改小過 → **repo 版落後，下次全選重貼會把手改值洗掉**（ScrToday 已有前科）。
- **待做**：從 Studio 匯出 ScrToday 現況，比對 `TemplateSize` 實際值，若與 repo 不同就更新 repo 檔。
- 來源：`ClosedTaskLog_2026-07-28`

---

## B0. 2026-08-03 夜間改造：巡檢不再改寫同事回覆（**已產出，待匯入驗證**）

### 根因
`0955`／`1015` 原本會在同事勾「1.」「5.」或未回覆時，把 `field_7` 整欄改寫成「3. 延遲…」或「未更新卡片，提醒更新。」——**系統覆蓋了同事自己填的內容**，事後看不出原始回覆。且 `1015` 的判定式有兩處寫錯（`本列原勾選1或5` 誤寫成 `'1.' or '4.'`、`本列未回覆` 漏 `not '5.'`），導致勾 4（已請假）被催辦、勾 5 的紀錄被抹掉。改造後這兩段整個移除，該 bug 隨之消失。

### 產出（`workingfiles/automate_import/`，全部 `suggestedCreationType: New`）
| 檔案 | 匯入後流程名 | 改動 |
|---|---|---|
| `0945_不寫回覆_v2_20260803.zip` | `0945_清單_每日專案延誤通知EMAIL_v2` | 移除 `條件_請假需標記` 整組；`要寄嗎` 移除 `not(outputs('本人請假'))` |
| `0955_PlannerStatus_v2_20260803.zip` | `0955_清單_再巡檢planner_v2` | 拆掉 `條件_1或5或空白`／`本列原勾選1或5`／`本列未回覆`，改為每列無條件寫 `PlannerStatus` |
| `1015_PlannerStatus_LINE_v2_20260803.zip` | `1015_清單_再巡檢planner_v2` | 同上；「建立工作」→ 寄信，與 0955 統一 |
| `1030_週報改讀清單_v2_20260803.zip` | `1030_工作延遲週報表_v2` | 資料源 Excel → 清單＋上週一~五區間篩選；統計基準統一 |
| `1000_彙整通知改讀清單_v2_20260804.zip` | `1000_清單_工作延遲彙整_通知行政email_v2` | 資料源 Excel table `{FCAE5C87}` → 清單今日資料；欄位改內部名（姓名 field_4／Email field_3／方案 field_5／任務 field_6）；篩選改「排除 `_TaskOnSchedule` 佔位列」；信件時間補 `addHours(utcNow(),8)` 台北時區（原本用 UTC，跨日時段會顯示前一天） |

畫面：`ScrToday_paste.pa.yaml`（選項改「待填寫假卡」）、`ScrAdmin_權限閘_paste.pa.yaml`（加 Planner狀態欄＋紅字），兩份都以使用者 08-03 匯出的 `*_0803bak` 為基準重建。

### 關鍵公式
- 寫入：`item/PlannerStatus = @if(outputs('本列狀態異動'),'已更新','延遲')`，`本列狀態異動 = empty(body('filter_本列仍逾期'))`。兩輪各自重讀 Planner 即時狀態並**無條件覆蓋**，語意＝「最後一次巡檢時的卡片狀態」。
- 通知對象：`PlannerStatus='延遲'` 且 `field_7` 為 `1.`／`5.`／空白，另排除 `_TaskOnSchedule` 佔位列。
- 行政頁 `NeedFollow` 與上式同義，驅動整列紅字；另有 `HasPlanStat` 區分「已更新」與「未巡檢」（欄位剛建時值全空，不判斷會整頁誤顯示「已更新」）。

### 為什麼統計改用前綴 `3.` 而非全文比對
清單既有資料存的是舊文字「請填寫假卡」，選項已改「待填寫假卡」。全文比對會讓上週資料全數落空——**舊版 1030 信二就是踩到這個坑**（比對字串 `'3.延遲 （09:30沒到公司，請填寫假卡）'` 與實際值差一個空格，靜默歸零，信二可能長期都在報「均有早加班」）。行政頁的 `NeedLeaveCard` 同一原因一併改前綴。

### ⚠ 改流程連接器時，連線宣告有**四處**必須同步（2026-08-03 實際踩到）
1030 首次匯入失敗：`PackageFlowMissingConnectionMap — 流程資源遺漏連線 'shared_excelonlinebusiness' 的對應`。原因是我只改了 `connectionsMap.json`／`apisMap.json`／`manifest.resources`，漏掉 `definition.json` 裡的 **`properties.connectionReferences`**——它仍宣告 Excel，但 map 已移除，匯入時對不起來。

四處清單（缺一不可）：
1. `definition.json` → `properties.connectionReferences`
2. `Microsoft.Flow/flows/<id>/connectionsMap.json`
3. `Microsoft.Flow/flows/<id>/apisMap.json`
4. `manifest.json` → `resources`（連線與 API 各一筆）＋ 流程資源的 `dependsOn`

**同一次檢查揪出 1015 的兩個同類問題**（比 1030 更隱蔽，匯入可能過、執行才爆）：把「建立工作」換成寄信後，`shared_office365` 沒有被加進任何一處宣告；而已不使用的 `shared_planner` 連線仍留著。修正時又踩到第二層：`shared_planner` 與 `shared_planner-1` 的**連線 id 不同但共用同一個 API resource id**，移除舊連線時把共用的 API 一起刪掉，導致 `dependsOn` 指向不存在的資源。

**日後改造流程的固定檢查**（腳本化的話，比對這三個集合）：`definition` 內實際出現的 `connectionName` ⊆ `connectionReferences` = `connectionsMap` = `apisMap`，且兩個 map 的所有 GUID 都存在於 `manifest.resources` 與 `dependsOn`。四支已全數複驗通過。

### 待驗證
- [ ] 四支匯入 Portal 實跑；舊流程先停用不刪，確認新的正常再處理。
- [ ] 建議順序：先讓 0955 跑一次寫進 `PlannerStatus`，再貼行政頁——否則一貼上整排「未巡檢」，看不出對錯。
- [ ] `ScrAdmin` 公式用的是**顯示名** `'Planner狀態'`；若該欄顯示名仍是英文，Studio 會報「名稱無效」，全部換成 `PlannerStatus`（去單引號）。App 端吃顯示名、流程端吃內部名，兩者不同層。
- [ ] 1015 改完後與 0955 結構完全相同，只差執行時間與信件文字，可考慮合併成一支跑兩輪（同 1000／1010 的情況）。驗證通過再議。

### B0-fix（2026-08-04）：0955／1015 主旨格式修正 — LINE 發不出去的真因
使用者回報「0955 與 1015 各收到一封信，但 LINE 沒發出」。Planner2Line 的 `email_parser.py` 的 `_SUBJECT_PATTERN` 把 `工作延遲_` 之後、日期之前的**全部字元**當人名，於是：

| 主旨 | 解析出的人名 | 比對結果 |
|---|---|---|
| `工作延遲_陳 怡惠_提醒未更新卡片與儀表板 \| 08/04` | `陳 怡惠_提醒未更新卡片與儀表板` | ✗ 找不到 |
| `工作延遲_陳 怡惠 \| 08/04` | `陳 怡惠` | ✓ 對到陳怡惠 |

**已修（2026-08-04 二次修訂）**：使用者在 Planner2Line 程式端調整了判別式，主旨改採**三段式**——`工作延遲_{姓名} | {MM/dd} | {說明}`，說明文字放回主旨但必須在日期之後。0955 尾段為「提醒未更新卡片與儀表板」、1015 為「第二次提醒未更新卡片與儀表板」。zip 已更新。

- 分隔符用**半形 `|` 前後各一空格**（與既有 definition 一致；解析文件裡顯示的全形「｜」是顯示轉換）。
- [ ] **待確認**：`0945` 的主旨仍是兩段式（無第三段）。新判別式若強制三段，0945 會跟著失效；相容兩段則不必動。

**這個 bug 在改造前就存在**：原版 0955 主旨帶 `_提醒未填line通知表單_`，同樣解析失敗——0955 的 LINE 提醒**可能長期都沒送達**（0945 主旨乾淨所以正常）。改造時照抄原結構把既有 bug 一起帶過來了。

**全案主旨前綴契約**（改任何一封信的主旨前先查這張表）：
| 前綴 | 用途 | 現況 |
|---|---|---|
| `工作延遲_{姓名} \| MM/dd \| {說明}` | Planner2Line 轉發個人 LINE。**人名與日期之間不可插入文字**；說明一律放第三段 | 0955／1015（0945 仍為兩段式） |
| `工作延遲彙整通知_…` | 給行政的彙整信，不觸發個人轉發 | 1000 彙整 |
| `工作延遲早加班狀況_…`／`上週 (…) 工作延遲彙整週報` | 週報 | 1030 |
| `米米提醒_{姓名}`（日期選填） | Planner2Line 的**第二種通知類型**，解析為 `type: "mimi"` | 解析端已支援，Automate 端未建 |
| `0945排程告警_名冊缺漏` | 告警（見 A6，**嚴禁用 `工作延遲_` 前綴**） | 未建 |

### 米米提醒（`米米提醒_` 前綴）─ 解析端已就緒 ✅（2026-08-04 使用者說明）
- 2026-07-02 由使用者提出並實作於 Planner2Line；規格紀錄在**該 repo 的** `_context/TaskLog_2026-07-02_米米提醒與日期選填.md`（不在本專案 archive）。
- 機制：主旨 `米米提醒_{人名}`（**日期選填**）→ 解析為 `type: "mimi"` → 套用 `config.json` 的 `mimi_message_template` → 發到該人名對應的 LINE，訊息開頭「提醒補發米米信」。
- **與工作延遲通知共用同一條轉發流程**，差別只在標題那一行。因此人名比對規則、找不到人的行為都與 `工作延遲_` 一致。
- **影響**：A8 末段「1005 的 `米米提醒_` 前綴路由待確認」**就此解除**——解析端早就支援，缺的只有 Power Automate 那一半。1005／1030 兩支流程（B2）可直接建，不必再等前綴確認。

### 0920 未動（本次所有改動的隱含依賴）
0920 是唯一建立列的流程，只寫 Title 與 field_1~6，本來就不碰 `field_7`／`field_8`。但下列三件事一改，四支流程都會靜默失效：`Title` 格式 `yyyyMMdd_Email_taskId`（1030 靠日期前綴篩選、0955／1015 靠結尾 taskId 比對卡片狀態）、`_TaskOnSchedule` 佔位列後綴、以及新建列的 `PlannerStatus` 為空（行政頁顯示「未巡檢」是正常銜接，不是漏寫）。

---

## B. 待執行（不需拍板，做就是了）

### B1. ScrAdmin 權限閘＋長文字截斷 yaml 貼上 ✅ 完成（2026-08-03 使用者回報已貼上、運作正常）
> `Wrap: =false` 與 `Tooltip` 可由 pa.yaml 貼入 `Label@2.5.1` 一事就此實證成立，先前標「（未驗證）」可解除。以下保留原驗收條目供日後重貼時對照。
- 檔案：`workingfiles/canvas/ScrAdmin_權限閘_paste.pa.yaml`（已含 07-23 權限閘 ＋ 07-31 症狀 5 的 `Wrap:=false`＋`Tooltip` 修法），**尚未貼進 Studio**。
- **貼上前**：確認基準檔——本檔以 07-23 上線版為基準改，若其後在 Studio 手動改過，先匯出現況比對再貼，否則洗掉手改部分。
- **貼上後逐條驗收**：
  1. `Screen.OnVisible` 補回（pa.yaml 帶不進畫面屬性）：
     `Set(gToday, Text(Today(), "yyyy/mm/dd")); UpdateContext({locIsAdmin: Trim(Coalesce(LookUp(team_member, Lower(Email) = Lower(User().Email), '出勤存取權限'), "")) = "行政管理"}); ClearCollect(colToday, Filter(AttendanceHistory, 日期 = gToday))`
  2. 長內容（MI-005 紅字、MI-007 請假字串）單行截斷、不再壓到相鄰列。
  3. 滑鼠停留在被截斷的儲存格看得到全文。
  4. 權限閘（非行政看不到內容）與下拉自動存仍正常。
- **（未驗證）** `Wrap: =false` 與 `Tooltip` 能否由 pa.yaml 貼入 `Label@2.5.1`。若 Studio 報屬性無效，先拿掉那兩行再貼，改用 `Left(..., N) & "…"` 手動截斷。
- 來源：`ClosedTaskLog_2026-07-28`、`ClosedTaskLog_2026-07-23`

### B2. 各 Power Automate 流程在 Portal 實際套用（指南都已寫好、狀態未回報）
| 流程 | 待套用內容 | 指南 |
|---|---|---|
| 0945 | §2-1 時間尾碼 `+08:00`→`Z`、§2-2 跨日年假 or 分支、§2-4 巢狀迴圈寫 `field_7=4` 的位置 | `Flow_請假整合_建置指南.html` |
| 0955 | §3-1 收斂號碼 `1或4`→`1或5`；另 A 修正（1/4/空白三者都查 Planner）＋ B 發卡尾段 | `Flow_請假整合_建置指南.html`、`Flow_0955_新增發卡提醒_建置指南.html` |
| 1005／1030 | 整支新建（未回報提醒＋發卡） | `Flow_1005_1030_米米信未回報提醒_建置指南.html` |
| 1010 | 是否需要同 1000 的兩項修法，待使用者提供匯出檔核對 | — |
| 1000 彙整通知行政 | 排定大修，不再疊補丁，`Flow_請假整合_建置指南.html` §5 已清空待重建規格 | — |
- 已完成不用再做：1000 米米信兩項修法（Z 尾碼＋FilterEvents 跨日分支）2026-07-07 使用者確認已套用、流程正常。
- 來源：`ClosedTaskLog_2026-07-06`、`ClosedTaskLog_2026-06-16`

### B2b.（新發現，2026-08-03）1000 的行事曆視窗尾碼仍是 `+08:00`，不是 `Z`
- 讀 2026-08-03 匯出檔確認：`取得事件的行事曆檢視_(V3)` 的參數是 `T00:01:00+08:00`／`T23:59:59+08:00`，與 lessons 記載「2026-07-07 已套用 Z 尾碼」**不符**。可能後來被改回，或當時只改到別支流程。
- 後果：查詢視窗與昨天的單日全天假重疊 8 小時，且現行 `FilterEvents` 沒有「主旨含今天日期」守衛 → **昨天請的單日全天假會被誤判成今天請假**。
- 修法已寫進 `Flow_1000_米米信代發解析_建置指南.html` §5（列為必改項⑤）。**1010 要一併確認。**

### B3. 清單索引：`Email`、`日期` 兩欄加索引
- `AttendanceHistory` 破 5000 列後，未編索引欄的篩選即使可委派也會失敗。這條在 06-16 就列了，一直沒做。
- 來源：`ClosedTaskLog_2026-06-16`、`ClosedTaskLog_2026-07-28`

### B4. 全檔掃同型委派 bug
- ScrAdmin 等其餘畫面的 Filter／LookUp／UpdateIf 逐條檢查委派警告，別只修爆掉的那一條。
- 來源：`ClosedTaskLog_2026-07-28`

### B5. 清除 ScrAdmin 畫布上重複的舊控件組
- 07-08 匯出檔證實 Studio 畫布疊了兩組座標完全重疊的控件（`conHeaderA` 與 `conHeaderA_1`…），舊那組標題還寫死 `2026/07/06`。留著會讓控件數與資料源存取加倍，且日後可能誤編輯到死掉那份。
- 來源：`ClosedTaskLog_2026-07-08`

### B6. 確認 App 分享範圍已放寬到全員
- UI 權限閘的前提是 App 分享給全員、由閘過濾。沒放寬的話，team_member 新增行政仍開不了 App。
- 併：同事改用 Safari／Chrome 開儀表板後回報結果；仍失敗才轉查分享權限。
- 來源：`ClosedTaskLog_2026-07-23`、`ClosedTaskLog_2026-07-28`

### B7. 兩份備份指南對齊 `rules/flow-guide-conventions.md`
- `Flow_清單轉TaskDB備份_建置指南.html`、`Flow_清單轉WeekDB複製_建置指南.html` 目前只做到「不要求回捲對照」，尚未套用「運算式帶 `@` 直接貼免點 fx」與浮動 🛠 修改待辦清單套件。下次碰這兩份時一併補。
- 來源：`ClosedTaskLog_2026-06-16`

### B8. 語音速記待辦（2026-07-08 進來，未處理）
- [ ] 清單內容備份到 excel（與 A3 每週搬移機制同一件事，可併）
- [ ] 10 點傳 LINE 到小工群組
- [ ] 移除聯絡人 LINE 通知
- [ ] 做一個管理後台
- 來源：`ClosedTaskLog_2026-07-08`

### B10.（2026-08-04 使用者指示）`@@` 人工註記規則：通知行政 ＋ 週報改判定

**背景**：系統不再自動判定「有沒有早加班」。改由行政在行政頁「行政人員輸入」的自訂欄位填 **`@@` 開頭**的字串寫進 `field_8`（實際出勤狀態），例：`@@未早加班，已補請假`。`@@` 是給系統認的前綴，用來把「行政查核結論」與同一欄裡系統寫入的米米信主旨、請假事件標題區分開。

**PO 定案（2026-08-04）**：
- `@@` **只用於「工作延遲且未於 09:30 早加班」一種狀況**。其他情形（已早加班、卡片忘記更新、一般到勤登記）用普通文字，不加前綴。前綴後面的原因文字可自由寫，統計不看。
- 週報「未早加班」**只認 `@@`**（口徑 1）。同事自己勾什麼不影響這個數字——**行政沒填，該筆就不會出現在週報上**，因為這件事本來就是行政要追辦的。
- 統計單位＝**件數**（沿用原措辭「上週有 N 件工作延遲未早加班之紀錄」），不改天數。

- [x] ~~更新 1030 週報的統計判定~~ ✅ **已改**：`本人未加班` 的 where 改為 `@startsWith(trim(coalesce(item()?['field_8'],'')),'@@')`；信一只列逾期件數、信二只列未早加班件數（兩封各司其職不重複）；全員無標記時信二附一句提醒確認行政是否漏填。檔案：`1030_週報改讀清單_v2_20260804.zip`。
- [x] ~~兩份文件同步~~ ✅ 已更新第四節、第五節與 mermaid 圖五。
- [ ] **通知行政人員這條規則**：三位「行政管理」權限者（黃文郁、塗文鳳、陳怡惠）都要知道 `@@` 只用於未早加班、以及沒填就等於沒發生。文件已寫好可直接發：`workingfiles/outputs/行政人員_出勤查核流程說明_2026-08-04.html`（第四節）。

**⚠ 週報主旨不可更動**（後續由 Planner2Line 解析轉發 LINE，已設程式解析）：
- 信一 `上周 (@{addDays(utcNow(),-7)} ~ @{addDays(utcNow(),-1)}) 工作延遲彙整周報`
- 信二 `工作延遲早加班狀況_上周 (…) ` ← **結尾有一個半形空格**
- 用字是「**周**」不是「週」，日期運算式沿用 `addDays(utcNow(), -7/-1)`。改造時我曾把這三處都改掉，2026-08-04 已逐字還原。

**輸入端處理（2026-08-04 定案：只做提示，不做下拉）**：`@@` 是少數事件、至今尚未發生過，加進 11 個常用下拉選項只會讓選單變雜。改為兩處提示，已寫入 `ScrAdmin_權限閘_paste.pa.yaml`：
- `txtNote.HintText`：`="自訂"` → `="自訂／未早加班打 @@"`
- `txtNote.Tooltip`（新增）：完整規則四行，含範例與「沒填就不計入」的提醒。

**打錯的容錯已做在流程端**（1030 的 `本人未加班`）：先去半形空格、再去全形空格、再把全形 `＠` 轉半形，最後比對前綴。`@@`／`＠＠`／`@＠`／`＠ ＠`／前置空格全部認得，單一個 `@` 不算。所以行政不必記得切輸入法。

**主旨日期範圍同步修正（2026-08-04）**：原本用 `addDays(utcNow(),-7) ~ addDays(utcNow(),-1)`＝上週一到上週日，與實際統計範圍（上週一到五）不一致，且假設一定在週一執行、手動補跑會偏移。已改為 `Compose_上週一 ~ addDays(Compose_上週一,4)`，與取資料的 `Compose_上週篩選` 同源。固定文字（「周」字、信二結尾半形空格、`工作延遲早加班狀況_` 前綴）維持原樣不動。

### B11.（2026-08-04 使用者指示）國定假日不計入 — **排定 2026-08-12（週三）處理**

**需求**：國定假日不執行巡檢、不建立清單資料、不列入統計。PO 定案：**補班日（週六上班）要一併處理**。

**方案（已與 PO 討論定案，只是還沒動手）**：只在源頭那支流程判斷，下游自然靜默——0930 不建立清單，0945 有「筆數>0」條件、0955／1015 篩選為空、2000 有「條件_有資料才寫」、1030 讀清單所以假日自然不列入。

| 步驟 | 內容 |
|---|---|
| 1 | 建 SharePoint 清單 `CompanyHolidays`：`Title` 存 `yyyyMMdd`（如 `20260928`），另一欄 `類型`＝`假日`／`補班` |
| 2 | `0930_清單_每日巡檢Planer進清單` 的 Recurrence **改成每天**（含週六日），開頭加：查表 → 判定 → 非工作日 `Terminate`（狀態 Succeeded，不產生失敗告警） |
| 3 | 判定式：在假日表→停；在補班表→跑；其餘看 `dayOfWeek` 是否 1..5 |
| 4 | 行政每年初把當年國定假日與補班日一次填進清單 |

**一併要處理**：1000 的兩支米米信流程讀的是信箱不是清單，假日仍會跑並寄空的彙整信給行政，要加同一組判斷擋掉。

**注意**：週五清理流程（`週五_清單清除兩週前資料_含備份查核`）用日曆日計算保留期，不受假日影響，不必改。

- [ ] 建 `CompanyHolidays` 清單並填入 2026 年假日／補班日
- [ ] 改 0930：Recurrence 改每天 ＋ 前置判定 ＋ Terminate
- [ ] 改 1000 兩支：加同一組判定
- [ ] 驗證：手動在假日表塞一筆當日日期，跑一次確認 Terminate 生效且不建資料

### B12.（2026-08-04 完成產出，待實跑驗證）清單保留期＋AttDB 備份機制

**背景**：A3 早已定案「清單最多留兩週」，但一直沒有實作。本次補齊「每日備份 → 每週清理」整套，並要求清理前先確認備份完成。

**五支流程（全部在 `workingfiles/automate_import/`，`suggestedCreationType: New`）**

| zip | 流程名 | 觸發 | 作用 |
|---|---|---|---|
| `清單清除舊資料_手動_20260804.zip` | `清單_清除舊資料_手動` | 手動 | 填截止日 `yyyyMMdd` ＋布林開關；先跑「否」看試算報告（筆數＋最舊/最新 5 筆），確認再跑「是」 |
| `清單全量複製到AttDB_手動_20260804.zip` | `清單_全量複製到AttDB_手動` | 手動 | 起訖日區間（留空＝不限）複製到 AttDB，歷史補檔用 |
| `清單全量複製到AttDB_日期文字版_20260804.zip` | 同上`_日期文字` | 手動 | 日期欄改 `@concat('''',item()?['field_2'])` 強制文字，**待測** |
| `2000_每日複製到AttDB_20260804.zip` | `2000_清單當日資料複製到AttDB` | 週一～五 20:00 | 複製當日資料（`startswith(Title,'yyyyMMdd_')`） |
| `週五_清單清除兩週前_含查核_20260804.zip` | `週五_清單清除兩週前資料_含備份查核` | 週五 20:30 | 界線＝今日−14 天 00:00（該日起保留）；刪前逐日查核 AttDB |

**AttDB 欄位對應**（使用者提供標題列，A→J）

| AttDB 欄 | 清單內部名 |
|---|---|
| ID (yyyyMMdd_Email) | Title |
| 員工編號 | field_1 |
| 日期 | field_2 |
| Email | field_3 |
| 姓名 | field_4 |
| 方案名稱 | field_5 |
| 任務名稱 | field_6 |
| 表單回覆 | field_7 |
| **Planner狀態** | **PlannerStatus** |
| 實際出勤狀態 | field_8 |

Excel 位置沿用 `清單轉WeekDB複製` 的 `source`／`drive`／`file`（`01YOOPXWNA7INQ4QDWMVEYTEMZ3AXUFDUL`＝`/TEST_PLANNER_CK/TaskDB.xlsx`），`table` 填名稱 `AttDB`（**非 GUID，未實測**；AttDB 是線上新建，本機三份 TaskDB 都沒有，GUID 無法離線取得）。

**查核信收件人**：`domain.e@techartgroup.com`；`ihueychen@techartgroup.com`（後者網域為推測）。三種結果都寄：清理完成／清理中止_資料尚未備份（列出缺哪幾天＋AttDB 日期樣本）／本次無待刪資料。

**本次修掉的兩個問題**
1. **分頁上限**：`minimumItemCount` 寫 100000 → 匯入報 `InvalidPaginationPolicy`（Maximum: 5000）。五支統一改 5000。代價：單次 GetItems 最多 5000 筆，全量複製要按月分批（超過的部分不報錯、只會漏掉）。
2. **AttDB 超過 5000 列時查核會誤判**：原設計是拉 AttDB 全表比對日期集合，5000 上限下舊日期讀不到 → 天天判定「未備份」而中止。改為**逐日查詢**：待刪日期去重後（通常 5 天），每天用 `$filter=日期 eq '...'` 查一筆，不受列數限制。

**待驗證**
- [ ] `table` 用名稱 `AttDB` 是否可行（失敗就在 Portal 重選表格，欄位對應不會掉）
- [ ] **AttDB 日期欄被 Excel 自動轉型**：使用者回報即使把儲存格設文字仍被轉。先試「選取表格欄（不是工作表欄）設文字」；無效改用日期文字版 zip；再無效改存 `20260721` 八碼（Excel 存成數字，永不轉日期）。**此事連動週五查核**——查核是字串比對 `日期 eq '2026/07/21'`，日期若成了日期型別就比對不到，流程會中止不刪並寄信（安全側，不會誤刪）。
- [ ] 五支實跑

**建議上線順序**：手動版把歷史補進 AttDB → 開 2000 跑一天確認 → 核對筆數 → 再開週五那支。

### B13.（2026-08-04）維運 SOP：真核域密碼變更後的連線修復

`workingfiles/outputs/維運SOP_出勤查核系統_2026-08-04.html`（已在 INDEX 讀取指引留指標）。

**關鍵事實**：連線是**帳號層級的共用資源**，不是每支流程各一份。同一種連線修一次，所有引用它的流程一起恢復——重點是七個連線都要修，不是逐支流程處理。

掃描 `automate_import/`＋`automate_export/` 全部套件的 `connectionReferences` 得到的連線清單：`sharepointonline`、`excelonlinebusiness`、`office365`、`office365groups`、`office365users`、`planner`、`planner-1`（Planner 兩個不同 id，都要檢查）。

文件含：觸發時機與症狀 → 影響範圍表 → Portal 修復步驟 → 三支代表流程的驗證（0930 驗 Planner/群組/使用者、1000_v2 驗 SharePoint/寄信、全量複製驗 Excel）→ Planner2Line 端憑證要另外更新 → Power Apps 不受影響 → 完成檢查表。

- [ ] Planner2Line 那節只寫「更新憑證、重啟、看 log」，**設定檔路徑與參數名待補**（該程式非本專案維護）

### B14.（2026-08-14）請假判定漏接「時數範圍」型主旨 — 已產出修正包，待匯入實跑 [Claude@Win]

**症狀**：2026-08-14 林軒請假，1000 流程沒把 field_8 寫進清單（列 ID 1769 空白）；同日林寬祐同樣是請假卻寫進去了。

**根因（讀 `1000_清單__米米信代發解析_v2_0814_20260814022953.zip` 的 definition 逐字確認，非推測）**：`套用至每一個 → FilterEvents` 的第二段條件只認三種情況——主旨含字面 `10:00`、含 `全天`、事件跨日超過 1 天。林軒的主旨是 `林軒請年假，2026-08-14(09:00-18:00)…`：

| 判斷 | 結果 |
|---|---|
| 含 `10:00` | ✗ 只有 `09:00-18:00`，字串不含 `10:00` |
| 含 `全天` | ✗ |
| 跨日 >1 天 | ✗ start `08-14T00:00`、end `08-15T00:00` 恰好＝1 天，`greater(1,1)`＝false |

三者皆假 → FilterEvents 空 → 走分支 C 不動。**差別只在請假系統把時數寫成範圍還是「全天」**，事件層資料是對的。

**`isAllDay` 不可當判準（實測否決）**：林軒 `(09:00-18:00)` 與林寬祐 `(全天)` 的 `isAllDay` 都是 `true`——請假系統把所有假都建成全天事件，時數只寫在主旨裡。若拿 `isAllDay` 當「涵蓋整天」，下午假（如 `13:00-18:00`）會被誤判成請假、蓋掉當事人自己的米米信。**唯一可靠的來源是主旨括號內的時段字串。**

**修法**：把「含字面 `10:00`」換成「解析括號內時段，判斷是否涵蓋 10:00」——
`起 ≤ '10:00' ≤ 迄`（零填補 HH:MM 的字串序＝時間序）。取括號段時串接哨兵 `(00:00-00:00)`，主旨沒有括號時不會索引越界且必然判 false；另先把全形 `（）：` 正規化為半形。

**產出（`workingfiles/automate_import/`）**

| zip | 基準 | 匯入方式 |
|---|---|---|
| `1000_米米信代發解析_v2_請假時段修正_20260814.zip` | 08-14 Portal 匯出的線上版 | manifest 為 `suggestedCreationType: Update`，匯入時選**更新既有流程**，GUID／顯示名／排程／連線全部沿用，不會產生第二支 |
| `0945_延誤通知EMAIL_v2_請假時段修正_20260814.zip` | 08-06 Portal 匯出的線上版 | 同上 |
| `1010_米米信代發解析_v2_請假時段修正_20260814.zip` | **由上面修好的 1000 包複製改造**（1010 匯出檔從未取得） | `suggestedCreationType: New`，會建出一支新流程；**測通後要停用舊的 1010，兩支不可同時啟用** |

**1010 包與 1000 包的差異（只有三處，其餘完全相同）**

| 項目 | 1000 | 1010 |
|---|---|---|
| 流程顯示名 | `1000_清單__米米信代發解析_v2` | `1010_清單__米米信代發解析_v2` |
| 觸發時間 | 週一~五 10:00（台北） | 週一~五 **10:10** |
| 收信上界動作 | `Compose_上界10`＝今日 10:01 | **`Compose_上界11`**＝今日 **11:00**（PO 2026-08-14 指定放寬並改名） |

動作改名連帶更新兩處引用：`取得電子郵件_(V3)` 的 `runAfter`、`篩選_今日米米信` 裡的 `outputs('Compose_上界11')`。全包已無 `Compose_上界10` 殘留。

上界運算式：`@formatDateTime(convertToUtc(addHours(startOfDay(convertFromUtc(utcNow(),'Taipei Standard Time')),11),'Taipei Standard Time'),'yyyy-MM-ddTHH:mm:ssZ')`

**⚠ 觸發在 10:10、上界卻是 11:00 → 上界實務上不會生效**：流程 10:10 就跑完，10:10~11:00 之間才到的信這輪根本讀不到。也就是「今日所有已收到的米米信都算」。若真要收到 11:00 的信，觸發時間得往後移；若只是要一個不卡人的寬鬆上界，現在這樣就對。

新舊 GUID、四處連線宣告（`connectionReferences` ⊆ `connectionsMap` = `apisMap`，且 GUID 全在 `manifest.resources` 與 `dependsOn`）已逐項複驗通過；舊 GUID 零殘留。

已驗（本機）：兩包除 `definition.json` 外每個檔 hash 與原包完全相同；`definition.json` 內除該一條 `where` 外位元完全相同；無 BOM、JSON 可解析。九組主旨的邏輯模擬全部符合預期——`(09:00-18:00)`／`(全天)`／`(09:30-10:00)`／全形括號版判請假，`(13:00-18:00)`／會議主旨／`會議(線上)` 不判請假。

**0945 從來不寫清單，本次修的是死碼**（08-06 線上匯出檔逐項查證）：整支流程的 operationId 只有 `GetItems` ×2、`GetEventsCalendarViewV3` ×1、`SendEmailV2` ×1——**沒有任何寫入動作**，全檔也搜不到 `field_7`／`field_8`。請假資訊在 0945 的唯一用途，過去是拿來「抑制寄信給請假的人」（更早的版本還會預填 `field_7='4.'`），但 B0 改造已依 PO 裁示把 `要寄嗎` 的 `not(本人請假)` 移除——**請假者現在照樣收延遲通知信**（PO：請假前本就該安排好工作、調整卡片截止日）。所以 `FilterEvents_本人請假` 與 `本人請假` 兩個動作現在是算了沒人用的死碼。修它只為兩支口徑一致、日後若重新接回不會又踩同一個坑；**不修也不影響現行行為**。

**未驗證**
- [ ] 兩包實際匯入 Portal 並手動執行一次
- [ ] `lessOrEquals` 對字串是否走序數比較（Logic Apps 文件列了 string 型別，但未實跑）。若不支援，改比 `int(replace(時分,':',''))`——注意不能直接改，`全天` 會讓 `int()` 炸掉整個 Query（Logic Apps 的函式引數是急切求值）
- [ ] 已知限制：時分若沒有零填補（`(9:00-18:00)`）字串序會失效。目前語料全部是零填補

**同型 bug 掃描（31 個 zip 全解壓比對，含 `informaiton/`）**：只有 0945 與 1000 兩系列含此判定，0955／1015／1030／2000／備份清理各支都沒有讀行事曆。

- [ ] **1010 未查**：匯出檔至今沒拿到（B2 已列）。它與 1000 結構相同、同樣寫 field_8，**極可能有同一個 bug，且會在 10:10 把 1000 寫對的值覆蓋掉**。要修得先從 Portal 匯出。
- [ ] 附帶發現：`workingfiles/automate_export/1000_清單__米米信行程通知寫入清單_20260707023203.zip` 只有 **49 bytes、無法解壓**（Drive 同步殘檔）。是 07-07 的舊版匯出，非現行版本，但要嘛重新匯出要嘛刪掉，別留著當可用檔。

### B9. ai-team CLI 協作版本補齊（跨專案的制度工作，非本案功能）
- 請 Claude／Antigravity 各補上自己的 ai-team CLI 協作版本（登入、短 prompt、卡住處理、避免直接讀寫檔案／權限需求、寫檔限制、編碼風險、signal 寫入慣例），補齊後推回 WTF repo 系統化成共用流程，再分享同事。
- 參考檔：`_context/ai-team-agent-cli-reference.html`
- 來源：舊 `INDEX.md`

---

## C. 已上線／已結案（不必再看，列此避免重複排查）

| 項目 | 狀態 |
|---|---|
| 模組一 MVP（Gallery＋Popup＋回寫 field_7） | ✅ 2026-06-15 實機驗證通過 |
| C1. ScrToday 委派退回抓不到今日資料 | ✅ 巢狀 Filter 已上線（07-31 使用者回報修好）；暫時解「非委派資料列限制 2000」保留無害 |
| ScrToday Popup 文字被裁（症狀 3） | ✅ `AutoHeight` 已上線 |
| ScrToday Gallery 卡片文字被裁（症狀 4） | ✅ 已上線；僅留白是否可接受待回報（A11） |
| 行政 App UI 權限閘（team_member 驅動） | ✅ 07-23 實測成功（清單層硬鎖仍缺，見 A2） |
| 1000 米米信 Z 尾碼＋跨日年假分支 | ✅ 07-07 Portal 已套用，流程正常 |
| 日期欄零填補 `yyyy/MM/dd`（0930 端＋App gToday 端） | ✅（推測）07-28 診斷時 `日期 = gToday` 比對正常，代表兩端格式已一致 |
| 0930 占位不寫入（條件左值錯＋附加陣列缺 `@`） | ✅ 已修並驗收 |
| 0945 名冊版寄信 | ✅ 已上線（缺漏告警未做，見 A6） |
| 短時段請假（如 09:30-10:00）＝整天抑制 | ✅ 2026-07-07 使用者確認接受此粒度，非 bug |

---

## 檔案異動（2026-08-03 本次整理）
- 新增：本檔、`_context/PRD_2026-08-03_出勤查核系統.html`。
- 重寫：`_context/INDEX.md`。
- 移入 `_context/archive/`：10 份 TaskLog（改前綴 `ClosedTaskLog_`）、`Handover_2026-06-07_出勤儀表板交接v1.md`、`Plan_2026-06-13`、`Plan_2026-06-15`、`AI_TEAM_DISCUSSION_2026-06-08`、`INBOX_已廢除_2026-06-20.md`。
- 保留在 `_context/`：`INDEX.md`、本 TaskLog、`lessons-learned.md`、`PRD_2026-08-03_出勤查核系統.html`、`AttendanceHistory.csv`（清單 schema 真相源）、`ai-team-agent-cli-reference.html`（B9 用）。
